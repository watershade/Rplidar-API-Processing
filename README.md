# Rplidar-API-Processing

This is a Rplidar A2 driver. Processing user can use it to grab data. But unfortunately It can not control pwm of rplidar because of unknown reasong of Serial class in processing.
